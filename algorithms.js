/** Class implementing the Algorithms on the data. */
class Algorithms {

    constructor(data, noc, ini_obs) {
        this.data = data;
        this.noc = noc;
        this.ini_obs = ini_obs; 
    }

    furthest_Sum() {

        let dimensions = [this.data.length, this.data[0].length];
        let index = [];

        for (let i = 0; i < this.data[0].length; i++) {
            index.push(i);
        }

        
        


    }


}